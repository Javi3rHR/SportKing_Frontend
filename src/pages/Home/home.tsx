import { Footer } from '@/components';
import Navbar from '../../components/pure/Navbar';

const Home = (): JSX.Element => {
	return (
		<>
			<Navbar />
			<Footer />
		</>
	);
};

export default Home;
